package com.pbogdev.sharedui.theme

import androidx.compose.ui.graphics.Color

// The Void (Backgrounds)
val RadarBlack = Color(0xFF000000)      // Pure black for the main screen
val RadarSurface = Color(0xFF121212)    // Slightly elevated black/dark gray for overlays/cards
val RadarSurfaceVariant = Color(0xFF1E1E1E) // For text fields and collapsed list items

// The Signal (Accents)
val NeonGreen = Color(0xFF00C86A)    // The classic glowing radar terminal green
val NeonGreenDim = Color(0xFF00330D)    // Dark washed-out green for ripples/faded states

// The Text (Content)
val TextWhite = Color(0xFFFFFFFF)       // Primary text (e.g., Match Percentage, Headers)
val TextGray = Color(0xFFA0A0A0)        // Secondary text (e.g., Timestamps, Subtitles)

// The Alerts (Errors / Disconnects)
val AlertRed = Color(0xFFD9385E)       // High-contrast tech red for errors or blocking actions